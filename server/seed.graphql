mutation {
  todo1: createTodoItem(data: { text: "Learn GraphQL", completed: false }) {
    id
  }
  todo2: createTodoItem(data: { text: "Learn React", completed: false }) {
    id
  }
  todo3: createTodoItem(data: { text: "Be awesome", completed: false }) {
    id
  }
}
